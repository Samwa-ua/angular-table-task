export interface BooksData {
    id: number;
    title: string;
    description: String;
    pageCount: number;
    publishDate: string;
}

export interface TransformedData {
    publishDate: string;
}